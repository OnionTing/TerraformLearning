# 
plugin_cache_dir   = "$HOME/.terraform.d/plugin-cache"
disable_checkpoint = true

# Set the credentials to use Terraform Enterprise
# You can go to TFE site --> Settings --> Teams, then create Team API Token, use the token to replace the "Your Team API Token" part
# more information about tokens can be found at https://www.terraform.io/docs/cloud/users-teams-organizations/api-tokens.html#organization-service-accounts
credentials "${var.hostname}" {
    token = "Your Team API Token"
}